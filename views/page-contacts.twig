{% extends "base.twig" %}

{% block content %}

<div class="page">

	<h1>{{page.title}}</h1>

	<div class="container">
		<div class="row">
			<div class="columns six offset-by-three contact-us">
				{{page.content}}
			</div>
		</div>
		<img class="contacts-radar" src="{{site.theme.link}}/img/radar.svg" alt="radar">
	</div>

</div>
    
{% endblock %}